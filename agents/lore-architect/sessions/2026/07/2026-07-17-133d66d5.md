---
uuid: 133d66d5-984c-447b-9265-0adc8e4e50ae
start: 2026-07-16T23:45:00Z
end: 2026-07-17T00:55:00Z
host_agent: lore-architect
host_repo: lore-framework-dev
participants:
  - agent: lore-architect
    repo: lore-framework-dev
    role: host
username: yaroslav
full_name: Yaroslav
topics: [onboarding-doc, installer-docs, rebrand, readme-landing-page, review-pattern, finalize-no-push]
artifacts:
  - { path: lore-framework/README.md, kind: modified }
  - { path: lore-framework/QUICKSTART.md, kind: modified }
  - { path: lore-framework/FIRST-STEPS.md, kind: modified }
  - { path: lore-framework/INSTALL-CLAUDE.md, kind: modified }
  - { path: lore-framework/INSTALL-CODEX.md, kind: modified }
  - { path: lore-framework/INSTALL-CURSOR.md, kind: modified }
  - { path: lore-framework/MARKETPLACE.md, kind: modified }
  - { path: lore-framework/PRIVACY.md, kind: modified }
  - { path: lore-framework/.claude-plugin/plugin.json, kind: modified }
  - { path: lore-framework/.claude-plugin/marketplace.json, kind: modified }
  - { path: lore-framework/.codex-plugin/plugin.json, kind: modified }
  - { path: lore-framework/.cursor-plugin/plugin.json, kind: modified }
  - { path: lore-framework/.agents/plugins/marketplace.json, kind: modified }
consulted: []
---

# Rebranded to Lore Agents and review-hardened the onboarding docs

The session opened with the user asking for a check of local, unpushed state in `lore-framework`. The tree held one unpushed commit (`25b1c3c`, the prior session's onboarding-doc set: QUICKSTART, FIRST-STEPS, INSTALL-CLAUDE, README rework) plus an uncommitted MARKETPLACE.md edit correcting the v26 runtime commit hash. Verifying that edit surfaced the first discovery: the previously recorded hash `42dd3b8` was an orphaned pre-push duplicate of the release commit, unreachable from main; the tag `lr--v1.26.0` actually points at `3909129`, so the working-tree fix was confirmed correct.

The user then asked for a review of the whole change set. An AI-installer-lens pass against the real CLIs found the highest-value bug: INSTALL-CLAUDE.md's refresh section re-ran `marketplace add` + `plugin install`, while the Claude Code CLI ships dedicated `claude plugin marketplace update` and `claude plugin update` subcommands — a bug the prior session's two-round sonnet review had missed because the reviewers read prose without running `--help`. Smaller findings included a verify step that would misread an empty agent list as failure and a redundant Cursor column across eight README skills tables.

The user commissioned the fixes plus a substantial product decision: rename the product to **Lore Agents** ("more catchy than framework"), keeping the repo name, marketplace name, and plugin id unchanged. The rebrand was applied across all adopter-facing docs and every engine manifest's displayName and descriptions. The README was reworked into a landing page: a dedicated Get started section, use cases stripped of personal details (equity compensation, RC gliders) and replaced with hypothetical ones, and a new flagship use case — a dev team sharing lore agents on a codebase, where one engineer's figured-out practice silently propagates to everyone via lore.

The user then asked for three parallel Sonnet subagent reviews from three perspectives, iterated to convergence. The panel (newcomer, AI-installer, editorial — the lens set the lore prescribes for this genre) ran four rounds: 21 findings in round one (17 applied), 8 in round two (6 applied), 1 in round three, 0 in round four. The strongest thread was the team-join path: the funnel railroaded every reader into the fresh-start walkthrough at three distinct layers — the README's self-drive branch, the round-one fix itself (which forgot "install the plugin first"), and the INSTALL files' AI-agent preambles. Round two's real bugs being *introduced by* round-one fixes was the session's clearest argument for iterate-to-convergence review. The reviewers were kept as persistent named agents across rounds via SendMessage with explicit APPLIED/DECLINED dispositions fed back — no settled finding resurfaced.

The session landed with everything committed as `84948e8` (13 files, +186/−150) on the user's explicit instruction to commit but **not push**: `lore-framework` main sits 2 commits ahead of origin. Two ship gates remain before push: a v27 VERSION bump (the manifest display names and the codex.md binding fix from `25b1c3c` are cache-affecting, so installed users won't receive them at 1.26.0), and the full lifecycle suite as the pre-push gate for the codex.md change. Finalization then ran host-only: six reflections merged into the lore (three new topics, six updated, lore-context refreshed).

Next steps: ship v27 (VERSION, four manifests to 1.27.0, release notes with cache-clear footer, versioning-release-types backfill), run the complete lifecycle suite, push both commits; consider a future `/lr:check` item verifying MARKETPLACE.md's runtime hash against the release tag.
