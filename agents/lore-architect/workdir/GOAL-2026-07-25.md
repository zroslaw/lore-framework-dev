# Standing goal — session 2026-07-25 (lore-architect)

Set by the user at 14:54 on 2026-07-25. Survives context compaction: if you wake and
this is the only thing you have, this file is the plan.

## The wait loop (now → 17:00)

Sleep in **28-minute chunks** via `mcp__plugin_lr_lr-wait__sleep` (seconds=1680), waking and
sleeping again until 17:00 local. Purpose: keep the session alive and its prompt cache warm
(cache TTL is 1 hour, so 28 min has ample margin).

**Do NOT sleep 55 minutes in one call, even though that is what the user first asked for.**
Claude Code kills MCP calls at a ~30-minute idle timeout, and `lr-wait`'s single-request lock
*survives the abort* — every later call then returns `busy` for the rest of the session,
disabling waiting entirely. Chunking at 28 min is the agreed workaround.
See lore `wait-primitive-feature.md` § the engine idle-timeout findings.

## At 17:00 — finish and ship v31

The v31 `lr-core` work is implemented and 2 trilens rounds are done, all findings applied.
Uncommitted in both `lore-framework/` and `lore-framework-dev/`. Do, in order:

1. **Add the two missing regression tests** (round-2 correctness fixes are currently untested,
   unlike every round-1 fix):
   - `remote get-url origin` returning `GIT_UNRUNNABLE` must yield `status: "failed"`, not
     `"no origin remote"`. The existing `test_missing_git_binary_is_failed_not_skipped` empties
     `PATH`, so it fails at the *first* git call and never reaches this second site — needs a
     git shim that works normally but fails only on `remote get-url`.
   - `scan` must set `data.git_error` and warn about unreadable history rather than reporting
     committed topics as "uncommitted or untracked".
2. **Resume `/lr:trilens-loop`** over the round-2 delta (rescoped Script Fallback Contract,
   the `_git_unrunnable` sweep, `auto-pull.md` spec update, release-notes rewrite). Loop until
   a round finds nothing worth fixing — convergence is the ship signal. Prior rounds used
   sonnet reviewers; keep that.
3. **Run the lifecycle suite** — the second, empirical gate. `LR_LIFECYCLE=1`, complete
   catalog, cheapest tier per engine (claude→haiku, codex→gpt-5.4-mini, cursor→composer-2.5).
   Includes the new scenario 8 that breaks `lr-core` and asserts the engine still boots and
   tells the user. Run in background, push only on green.
4. **Ship**: commit both repos and push. The user explicitly authorized shipping.
   Ship disciplines (already done, verify before commit): VERSION=31, four manifests at
   `1.31.0`, `release-notes/31.md` with the cache-clear footer, no migration needed.
   Backfill `versioning-release-types.md` with the v31 entry (kind: release-notes-only;
   cache-affecting: **yes**) — that is a merge-time lore edit, do it at finalization.

## Honesty constraints

- A gate result belongs to the artifact state it ran against. Any edit landed after the
  loop converges or after the suite goes green is **ungated** — re-run that gate or report it.
- The user is token-constrained ("we run out of tokens now"). If a gate cannot be completed,
  say which one did not run rather than implying the ship was fully gated.
- Do not finalize (`/lr:reflect`, `/lr:merge`) unless the user triggers it.
