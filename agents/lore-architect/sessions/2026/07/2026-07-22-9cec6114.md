---
uuid: 9cec6114-ac75-4b86-8821-8716669429a7
start: 2026-07-21T10:00:00Z
end: 2026-07-22T01:14:16Z
host_agent: lore-architect
host_repo: lore-framework-dev
participants:
  - agent: lore-architect
    repo: lore-framework-dev
    role: host
username: yaroslav
full_name: Yaroslav
topics: [v28-release-prep, session-archive, lifecycle-testing, lore-beings, release-gates]
artifacts:
  - { path: lore-framework/docs/summarize.md, kind: modified }
  - { path: lore-framework/docs/finalize.md, kind: modified }
  - { path: lore-framework/docs/create-repo.md, kind: modified }
  - { path: lore-framework/docs/create-agent.md, kind: modified }
  - { path: lore-framework/scripts/session-takeover, kind: modified }
  - { path: lore-framework/scripts/lrb.py, kind: modified }
  - { path: lore-framework/release-notes/28.md, kind: modified }
  - { path: lore-framework-dev/tests/test_session_archive.py, kind: created }
  - { path: lore-framework-dev/tests/fixtures/archive_fixture.py, kind: created }
  - { path: lore-framework-dev/tests/lifecycle/harness.py, kind: modified }
  - { path: lore-framework-dev/tests/lifecycle/test_finalize.py, kind: modified }
  - { path: lore-framework-dev/tests/lifecycle/test_lrb_lifecycle.py, kind: modified }
  - { path: lore-framework-dev/agents/lore-architect/lore/lifecycle-testing-harness.md, kind: modified }
consulted: []
---

# Prepared v28 archive release

The session opened with the user asking the lore architect to merge the recent session archive and usage-tracking feature into v28 and run the preship e2e tests. The starting state was a local v28 Lore Beings release line plus a separate feature branch containing `session-takeover archive`, archive/usage frontmatter, and dev-side tests. The feature was replayed into the v28 worktree and committed as `lore-framework` commit `5e00209` and `lore-framework-dev` commit `10902d3`.

The substantive work landed the archive pipeline in `scripts/session-takeover`, added raw transcript gzip archives plus stats JSON, updated `docs/summarize.md` and `docs/finalize.md`, strengthened `lrb.py` process reaping, refreshed `release-notes/28.md`, and added deterministic archive tests. The dev harness gained archive assertions, Keeper B2/B3 coverage, cheaper default model tiers, and stronger create-repo/create-agent prompts that verify expected files before printing `DONE`.

The session had several corrections. The user explicitly rejected using expensive default models for e2e: the gate now defaults to Claude Code `haiku`, Codex `gpt-5.4-mini`, and Cursor `composer-2.5`, with quality regular defaults aligned too. Real e2e then found two cheap-tier fidelity issues: Haiku skipped the new summarize Step 1.5 inside full finalize until `finalize.md` made the archive attempt a pre-commit verification, and Haiku sometimes printed `DONE` for create-repo without creating files until the create docs and harness prompts made file-state verification explicit.

Where it landed: deterministic tests passed (`183` tests, with gated lifecycle tests skipped), `diff --check` stayed clean, Keeper/lrb e2e passed by engine, and the newly failing targeted standard lifecycle scenarios passed after doc/prompt fixes. The full standard lifecycle gate did not get a final clean three-engine pass: Claude hit the account-limit signature mid-rerun, Codex and Cursor had transient/flaky broad-suite failures whose targeted reruns passed, and the user stopped an unnecessary Codex rerun. The final lore update preserved the distinction between a release-prep checkpoint that is safe to commit and a release-green state that is safe to push/tag.

Next steps are to decide whether to accept the targeted e2e evidence plus provider-limit notes as sufficient for shipping v28, or wait for clean full standard lifecycle reruns on all engines. Until that decision, the release is committed locally but should not be described as fully release-green.
