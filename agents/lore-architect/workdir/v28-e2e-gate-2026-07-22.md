# v28 Standard Lifecycle E2E Gate — 2026-07-22

Framework candidate under test: `lore-framework` commit `5e00209`
(`Prepare v28 session archive and keeper hardening`).

Dev harness state: `lore-framework-dev` main with local lifecycle runner changes
(`tests/lifecycle/run_matrix.py`) applied.

## Free Precheck

Command:

```bash
python3 -m unittest discover -s tests -v
```

Result: `183` tests run, `OK`, `36` skipped because lifecycle gates were disabled.

## Broad Standard Lifecycle Matrix

Command:

```bash
LR_LIFECYCLE=1 \
LR_FRAMEWORK_DIR=/Users/yaroslav/Documents/agent-workspace/lore-framework \
python3 tests/lifecycle/run_matrix.py \
  --engine-jobs 3 \
  --module-jobs 1 \
  --results-dir tests/lifecycle/results/v28-5e00209
```

Result path:

```text
tests/lifecycle/results/v28-5e00209/20260722T094400Z-standard/summary.json
```

Result: `16/18` module runs passed.

Passed:

- Claude/haiku: consult+attach, finalize, recall, repo/workspace, takeover.
- Codex/gpt-5.4-mini: boot, consult+attach, finalize, recall, repo/workspace, takeover.
- Cursor/composer-2.5: boot, consult+attach, finalize, recall, takeover.

Broad-run failures:

- Claude/haiku `test_boot.py`: scenario 05 did not stamp `lore-repo.md` from `17` to `28`.
- Cursor/composer-2.5 `test_repo_workspace.py`: scenario 21 (`/lr:update --dry-run`) timed out at 420s.

## Targeted Reruns

Claude boot scenario 05:

```bash
LR_LIFECYCLE=1 \
LR_FRAMEWORK_DIR=/Users/yaroslav/Documents/agent-workspace/lore-framework \
python3 tests/lifecycle/run_matrix.py \
  --engines claude \
  --modules test_boot.py \
  --test-filter 05 \
  --results-dir tests/lifecycle/results/v28-5e00209-targeted-claude-boot05
```

Result path:

```text
tests/lifecycle/results/v28-5e00209-targeted-claude-boot05/20260722T103929Z-standard/summary.json
```

Result: passed (`1/1`, 97.0s, haiku).

Cursor update dry-run scenario 21:

```bash
LR_LIFECYCLE=1 \
LR_FRAMEWORK_DIR=/Users/yaroslav/Documents/agent-workspace/lore-framework \
python3 tests/lifecycle/run_matrix.py \
  --engines cursor \
  --modules test_repo_workspace.py \
  --test-filter 21 \
  --results-dir tests/lifecycle/results/v28-5e00209-targeted-cursor-update21
```

Result path:

```text
tests/lifecycle/results/v28-5e00209-targeted-cursor-update21/20260722T104124Z-standard/summary.json
```

Result: passed (`1/1`, 147.7s, composer-2.5).

## Interpretation

This is not a clean broad-matrix green: the broad run had two failed modules.
Both failed cases passed when rerun in isolation with the same framework candidate
and cheapest recommended engine models.

Operational interpretation: v28 has strong targeted evidence and no persistent
reproduction of the broad-run failures, but the release decision still needs an
explicit acceptance of "broad failures passed on targeted rerun" as sufficient
gate evidence. If requiring strict green, rerun the full matrix after the runner
changes are committed; otherwise this note is the durable evidence pointer for a
targeted-evidence release decision.
